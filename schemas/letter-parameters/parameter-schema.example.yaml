jurisdiction: "{{JURISDICTION}}"
discipline: "structural"
letter_type: "focused-observation-summary"
project_metadata:
  name: "{{PROJECT_NAME}}"
  address: "{{PROJECT_ADDRESS}}"
  permit_reference: "{{PERMIT_REFERENCE}}"
inspection_context:
  date: "{{INSPECTION_DATE}}"
  method: "visual"
  observed_areas:
    - "{{OBSERVED_AREA}}"
  access_limitations:
    - "No destructive testing"
scope_of_review: "Limited to observed condition at accessible areas"
assumptions:
  - "Concealed elements were not assessed"
limitations:
  - "This draft does not provide final code compliance certification"
requested_by: "{{REQUESTING_PARTY_ROLE}}"
pe_reviewer: "{{PE_REVIEWER_NAME_OR_ROLE}}"
